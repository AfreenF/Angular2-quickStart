import { Component } from '@angular/core';			// import the members that we need in our application.
@Component({
  selector: 'my-app',
  template: '<h1>My First Angular 2 App. Hello World</h1>'
})   // this is the component metadata defined by decorator, and template.
export class AppComponent { } // this is the class, it defines the properties and methods needed by our view.
